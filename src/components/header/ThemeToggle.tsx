'use client'; import React, { useEffect, useState } from 'react'; export function ThemeToggle() { const [mounted, setMounted] = useState(false); useEffect(() => setMounted(true), []); const [theme, setTheme] = useState(() => (typeof localStorage !== 'undefined' ? localStorage.getItem('theme') || 'dark' : 'dark')); useEffect(() => { const root = window.document.documentElement; if (theme === 'dark') { root.classList.add('dark'); } else { root.classList.remove('dark'); } localStorage.setItem('theme', theme); }, [theme]); const toggleTheme = () => { setTheme(theme === 'light' ? 'dark' : 'light'); }; return ( <button onClick={toggleTheme} aria-label='Toggle theme' className='p-2 rounded-xl bg-gray-800 text-yellow-400 hover:bg-gray-700 transition-all shadow-md flex items-center justify-center w-10 h-10 cursor-pointer'> {mounted ? ( theme === 'dark' ? ( <span role='img' aria-label='Light mode' className='text-xl'>☀️</span> ) : ( <span role='img' aria-label='Dark mode' className='text-xl'>🌙</span> ) ) : ( <span className='text-xl'>⏳</span> )} </button> ); } export default ThemeToggle;