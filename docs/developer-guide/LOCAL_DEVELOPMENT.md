# Local Development Guide

- Prefer feature folders under src/features for UI
- Put shared engines under src/*-engine
- Use design tokens (CSS variables) — avoid hardcoded colors
- Register tools via toolConfigurationManager + optional logic module
- Run pnpm typecheck before pushing
