# Security Handbook v1.0

Middleware applies buildSecurityHeaders (CSP, HSTS prod, nosniff, frame deny).
Validators + sanitizers centralized. Rate-limit ready.
ConsentManager: analytics off by default. No secrets in NEXT_PUBLIC_*.
