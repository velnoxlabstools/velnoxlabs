# Testing Guide

```bash
pnpm test:unit
pnpm test:integration
pnpm test:coverage
pnpm test:e2e
```

Structure: tests/unit, tests/integration, tests/e2e, fixtures, mocks.

Policy: every bugfix adds a regression test.
