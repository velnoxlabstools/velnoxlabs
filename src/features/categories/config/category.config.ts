import type { CategoryPageConfig } from '@/types/category-page';

export const categoryPageConfig: CategoryPageConfig = {
  pageSize: 12,
  defaultSort: 'order',
  showSidebar: true,
  showFilters: true,
  relatedLimit: 4,
};
