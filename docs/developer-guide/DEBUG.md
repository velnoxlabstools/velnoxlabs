# Debug Guide

- Use browser React DevTools for providers
- Security logger redacts secrets in dev console
- monitoringService.snapshot() for diagnostics
- recoveryManager.listSnapshots() after backup tests
